# 本地凭证 / 密钥（永远不要提交）
# .env* 兜住所有 .env 变体（含 .env.save / .env.bak.* 等备份），仅放行示例文件
.env*
!.env.example
*.pem
*.key
secrets/

# 编辑器 / OS
.idea/
.vscode/
.DS_Store

# 编译产物
/bin/
/dist/
*.exe
*.out

# 日志 / 临时文件
*.log
/tmp/

# 误落在根目录的服务端二进制（生产部署应该用 /tmp/drama-api）
/drama-api
